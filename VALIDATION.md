# 本地验证 · 百科框架迁移 · 2026-09-07

## 迁移前快照

- 首个提交 `d7a4c2fdd145e4fcd23ab76b1e2434b8dd309d13`。
- AuthorDate 与 CommitDate 均为 `2026-09-06T20:00:58+08:00`，按用户要求在 20:00 附近随机选择。
- 提交包含现有源码、测试、文档、依赖清单与忽略配置；排除生成的 dist、node_modules 和本地协调状态。初始提交后工作区干净，未配置或推送远程。

## 当前迁移

- `npm test`：18 项通过。原有 13 项专题测试全部随专题迁移并保持可运行；新增目录登记、合法路由、草稿排除、组合检索、URL 恢复、分批条目及主题优先级检查。
- `npm run build`：通过。输出目录首页与专题的真实 HTML 入口，检查入口引用的资源均存在。
- 比较初始提交与迁移后的文件：物理、Worker、渲染、音频、故事和原有专题 CSS 字节一致；专题入口只增加公共导航与配色适配。
- 首页专属 JS 为 4,760 bytes，共享 JS 为 3,161 bytes（未压缩）。首页生产依赖没有模拟器或 Worker。黑洞专题包仍约 598 kB，保留构建体积提示。
- `git diff --check`：通过。迁移改动保留工作区，未包含在上述初始提交中。

## 实际浏览器

- 在 localhost:4173 的构建预览中从目录进入 `/topics/black-hole/`，专题正常启动。
- 切换浅色，确认阅读区域为浅色、黑洞画布仍为深色。返回目录后外观偏好保留；恢复“跟随内容”后，宇宙分类自动深色，全部目录恢复浅色。
- 选择瓦解路线，后台模型就绪，时间轴定位 01:12，切换学术版仍保留进度；继续播放至 01:41，模型内落读数由 22.6% 增至 43.5%，原有连续演示可运行。
- 检查无结果搜索、分类与搜索组合、清除筛选、URL 中的分类和查询参数。
- 390 × 844 手机视口下检查首页与专题：导航和分类可用，首页 scrollWidth 与 innerWidth 均为 390；浅色专题画布正常。已恢复默认桌面视口。
- 返回目录后页面无 canvas；正式首页只加载目录入口。浏览器 warn/error 日志为空。
- 最终封面移除可能被裁切的边角文字，重新构建并打开正式目录；专题与共享代码哈希未改变，复用已完成的专题交互验证。
- localhost:5173 开发首页已打开供用户查看。临时构建预览会关闭。

## 最终构建身份

- `dist/assets/home-DBWjCEIc.js`：SHA-256 `4f8538c11fbef8c4725a2be73e2bf61b663b80ccbffdcd8c8b1fb7ce26116caa`。
- `dist/assets/black-hole-C_YPxekT.js`：SHA-256 `6e3681b87042178474a179f059bf9b8d5f2d234c7701465fa6c19ee7090d2c87`。
- `dist/assets/disruptionWorker-nKdFGxSx.js`：SHA-256 `1411b2819a4919504386f9e16e657be8dafcd5ca0de558401a9f27b202835701`。

## 新专题科学与交互修正 · 2026-09-08

- 在用户新增的太阳系、地球四季专题上继续修改；未提交、未推送。保留原有目录测试修改。
- `npm test`：35 项通过。新增五项回归测试，覆盖渲染坐标变换与城市光照一致性、夏至/冬至北极完整自转周期、零倾角极点退化、椭圆焦点与周期复现、等面积速度及近远日点速度。
- `npm run build`：通过。保留 Three.js 共享块约 561 kB 的体积提示。`git diff --check` 通过。
- 通过 localhost:4173 的生产构建预览检查两个主题，默认暂停。地球夏至北极显示白天、23.4°、24.0 小时；冬至为 0.0 小时；零倾角显示晨昏交界与地平线提示。选择城市不改变时刻，播放中城市高度与投影持续刷新。
- 地球和太阳系播放均推进；把时间轴定位到 500 后恢复“继续”按钮并停在所选位置。太阳系学术读数由所选行星计算，水星为 0.9996。
- 目视确认太阳等比例完整入镜、“只看行星”的标签与土星环、地球放大的公转视图、太阳视线近景、直射斜射侧向光线。统一直径使用正交投影，公转画面另有轨道间距压缩。
- 390 × 844 视口下两个主题的 document scrollWidth 与 innerWidth 均为 390，画布宽 356；已恢复默认桌面视口。固定画布高度消除 canvas 固有尺寸对 flex 布局的反馈增长。
- 目视确认浅色标题可读、三维画面保持深色；恢复“跟随内容”。浏览器 warn/error 日志为空。
- 测试证明几何模型和本地交互，不代表真实日期星历、天气预报或完整多体仿真；这些范围已写入页面说明与专题 README。

## 教学对照与视觉资源 · 2026-09-08

- 新增地球页首的独立倾角实验。小地球上的纬度圈、展开的日侧/夜侧路程、正午太阳高度使用相同的日照几何。提供 0° 对照、0–23.44° 滑块、北京/悉尼/赤道/北极圈/北极点、四个节气和一次自转动画。极点的纬度圈退化为点，普通昼长不适用时显示灰色与地平线提示。
- 全套测试先通过 38 项；之后补充小地球与展开纬度圈的一致性，相关实验文件 4 项通过。其余测试输入未改变，复用前次结果；当前总计 39 项。测试以整圈采样核对解析昼长，验证南北半球相反变化与极点边界。
- 生产构建通过；最后仅新增封面与绘图线宽/颜色，复用已经通过的物理测试。`git diff --check` 通过。
- Solar System Scope / INOVE 的原始 2k 贴图随主题打包：八大行星、地球云图、月球、土星环。页面与 assets/ATTRIBUTION.md 注明 CC BY 4.0、来源及球面映射/补光/云图叠加。加载失败保留基础色；离页释放纹理并拒绝过期加载回调。
- 三个 cover-v2.jpg 为 ImageGen 生成的说明性插图，转为 1200 像素宽 JPEG；原有 cover.svg 保留。首页优先选择新封面，旧主题可回退 SVG。
- 生产预览目视核对：小地球地轴倾斜、日侧纬度圈、展开路程与正午高度；北京夏至 14.8 h、冬至 9.2 h、倾角归零左右 12 h；悉尼冬至 14.3 h；北极极昼和地平线提示。一天动画结束后停在正午。
- 生产预览目视核对木星云带、地球大陆/云层、水星陨石坑、土星环，以及三个首页封面的加载与裁切。console warn/error 为空。
- 390 × 844 浅色视口：实验对照卡片改为上下排列，页面 scrollWidth=innerWidth=390，两张实验画布均宽 356。已恢复桌面视口和跟随内容外观。
- 保留工作区修改，未提交或推送。教学设计与几何验证不等于已经进行儿童可理解性测试。
